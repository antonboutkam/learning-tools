{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.local/types/wat-hoort-bij-wat/v1/schema.json",
  "title": "Wat hoort bij wat v1",
  "description": "Koppel-oefening waarbij studenten items uit de linker kolom verbinden met de juiste items uit de rechter kolom. Je kunt ook extra afleiders toevoegen door alleen een rechter-item op te geven.",
  "type": "object",
  "required": ["title", "pairs"],
  "additionalProperties": false,
  "properties": {
    "title": {
      "type": "string",
      "title": "Titel",
      "description": "Korte titel die als kop boven de opdracht wordt getoond."
    },
    "description": {
      "type": "string",
      "title": "Uitleg",
      "description": "Korte uitleg of instructie onder de titel.",
      "format": "textarea"
    },
    "showCheck": {
      "type": "boolean",
      "title": "Toon controleknop",
      "description": "Toon een knop waarmee studenten hun koppelingen kunnen controleren.",
      "default": true
    },
    "showCorrectOnCheck": {
      "type": "boolean",
      "title": "Toon feedback bij controleren",
      "description": "Markeer linker-items als goed/fout na het controleren.",
      "default": true
    },
    "shuffleOptions": {
      "type": "boolean",
      "title": "Shuffle rechterkolom",
      "description": "Zet de opties in de rechter kolom in willekeurige volgorde.",
      "default": true
    },
    "pairs": {
      "type": "array",
      "title": "Koppels",
      "description": "Lijst met koppels (links ↔ rechts). Laat `left` leeg of weg om een extra (incorrect) antwoord alleen in de rechter kolom te tonen.",
      "minItems": 2,
      "items": {
        "type": "object",
        "title": "Koppel",
        "required": ["right"],
        "additionalProperties": false,
        "properties": {
          "left": {
            "type": "string",
            "title": "Links",
            "description": "De term/vraag in de linker kolom. Laat leeg (of laat dit veld weg) om alleen een afleider in de rechter kolom toe te voegen."
          },
          "right": {
            "type": "string",
            "title": "Rechts",
            "description": "Het antwoord in de rechter kolom. Dit veld is verplicht, ook voor afleiders."
          }
        }
      }
    }
  }
}

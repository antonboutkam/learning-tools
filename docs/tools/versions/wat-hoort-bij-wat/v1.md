# Wat hoort bij wat (v1)

> Agentkaart. Bron: [catalog.json](../../catalog.json); gegenereerd met `node scripts/build-tool-docs.mjs`. Werk bij inhoudelijke wijzigingen eerst de catalogus, het schema en de runtime bij.

## Toepassingen

- Begrippen koppelen
- Woordenschat en definities oefenen

## Features

- Twee kolommen verbinden
- Afleiders
- Optioneel schudden
- Nakijken en correcte koppelingen

## Implementatie

app.js bouwt de koppelinterface uit pairs en beoordeelt de lokale verbindingen.

- Runtime: [types/wat-hoort-bij-wat/v1/](/types/wat-hoort-bij-wat/v1/)
- Configuratieschema: [schema.json](/types/wat-hoort-bij-wat/v1/schema.json)
- Voorbeeld: [example.json](/types/wat-hoort-bij-wat/v1/example.json)

## Configuratie

Gebruik de JSON die via `?data=<publieke-json-url>` wordt geladen. De schema-URL uit de registry is de norm; de lijst hieronder is er voor snel agent-overzicht.

- `title` (verplicht) — Titel: Korte titel die als kop boven de opdracht wordt getoond.
- `description` (optioneel) — Uitleg: Korte uitleg of instructie onder de titel.
- `showCheck` (optioneel) — Toon controleknop: Toon een knop waarmee studenten hun koppelingen kunnen controleren.
- `showCorrectOnCheck` (optioneel) — Toon feedback bij controleren: Markeer linker-items als goed/fout na het controleren.
- `shuffleOptions` (optioneel) — Shuffle rechterkolom: Zet de opties in de rechter kolom in willekeurige volgorde.
- `shuffleLeftColumn` (optioneel) — Shuffle linkerkolom: Zet de items in de linker kolom in willekeurige volgorde.
- `pairs` (verplicht) — Koppels: Lijst met koppels (links ↔ rechts). Laat `left` leeg of weg om een extra (incorrect) antwoord alleen in de rechter kolom te tonen.

## API

Statische iframe-API; geen eigen server-endpoint of score-passback.

Algemene iframe-contracten:

- Launch: `/types/wat-hoort-bij-wat/v1/?data=<url-encoded JSON-URL>`.
- `unique_id` kan als queryparameter worden meegegeven wanneer de tool instantiecontext of lokale opslag gebruikt; de schema-eis blijft leidend.
- De registry levert `launchUrl`, `schemaUrl` en `exampleDataUrl`; gebruik nooit een onversieerde tool-URL.
- Standalone tools zenden geen `postMessage` en geen Canvas-score terug. Zie [LTI-inzendingen](../../lti-submission.md) voor de grens van die integratie.

# Bin/Hex/Dec Reken (v1)

> Agentkaart. Bron: [catalog.json](../../catalog.json); gegenereerd met `node scripts/build-tool-docs.mjs`. Werk bij inhoudelijke wijzigingen eerst de catalogus, het schema en de runtime bij.

## Toepassingen

- Getalstelsels oefenen
- Binair, decimaal en hexadecimaal vergelijken

## Features

- Kassastijl toetsenbord
- Bin/dec/hex-preview
- Instelbare bytegrootte

## Implementatie

Zelfstandige oefening in app.js; laadt JSON-configuratie en houdt de invoer alleen in het actieve scherm.

- Runtime: [types/bin-hex-dec-reken/v1/](/types/bin-hex-dec-reken/v1/)
- Configuratieschema: [schema.json](/types/bin-hex-dec-reken/v1/schema.json)
- Voorbeeld: [example.json](/types/bin-hex-dec-reken/v1/example.json)

## Configuratie

Gebruik de JSON die via `?data=<publieke-json-url>` wordt geladen. De schema-URL uit de registry is de norm; de lijst hieronder is er voor snel agent-overzicht.

- `title` (optioneel) — Titel: Titel die boven de opdracht wordt getoond.
- `input` (verplicht) — Invoerformaat: Het formaat waarin het opgave-getal wordt getoond.
- `output` (verplicht) — Uitvoerformaat: Het formaat waarin de student het antwoord moet invoeren.
- `byte_size` (verplicht) — Byte-size (bits): Beschikbaar bitbereik voor de oefening. Dit bepaalt ook het aantal posities op het teller-scherm.
- `preview` (verplicht) — Preview tijdens invoer: Toon tijdens typen direct hoe de huidige invoer eruitziet in BIN/DEC/HEX.
- `unique_id` (verplicht) — Unique ID: Unieke sleutel voor voortgang/certificaatopslag van deze opdracht.
- `getal` (verplicht) — Getal: Het opgave-getal in hetzelfde formaat als `input` (bijvoorbeeld BIN: 10101101, HEX: AD, DEC: 173).

## API

Statische iframe-API; geen eigen server-endpoint of score-passback.

Algemene iframe-contracten:

- Launch: `/types/bin-hex-dec-reken/v1/?data=<url-encoded JSON-URL>`.
- `unique_id` kan als queryparameter worden meegegeven wanneer de tool instantiecontext of lokale opslag gebruikt; de schema-eis blijft leidend.
- De registry levert `launchUrl`, `schemaUrl` en `exampleDataUrl`; gebruik nooit een onversieerde tool-URL.
- Standalone tools zenden geen `postMessage` en geen Canvas-score terug. Zie [LTI-inzendingen](../../lti-submission.md) voor de grens van die integratie.
